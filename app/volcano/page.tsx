/**
 * Volcano Island: /volcano.
 *
 * The same page as a room; the game boots onto the island, so a reload or a
 * shared link lands on the beach rather than on the world map's dock.
 */
export { default } from "../page";
